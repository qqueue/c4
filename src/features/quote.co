<-! onready

listen $ \threads .on \click '.permalink .id' !(e) ->
  selection = window.getSelection!toString!trim!
  selection = ">#selection\n" if selection

  $ \comment
    &value += ">>#{@textContent}\n#selection"
    &focus!

  e.preventDefault!
