number = null
text = null

setTitle = ->
	document.title = 
		if number then "(#number) #text" else text

Object.defineProperties titler,
	number:
		get: -> number
		set: ->
			number := it
			setTitle!
	text:
		get: -> text
		set: -> 
			text := it
			setTitle!


