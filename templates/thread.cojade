
#{@@container}(id="#{@id or 't' + @no}", data-no="#{@no}", class="#{@classes ? ''} #{@@thread-classes locals}")
  = @@post-template @op, \div \op @@post-classes
  .thread-info
    | #{(@omitted?replies or 0) + @replies.length} replies and #{(@omitted?imageReplies or 0) + @imageReplies.length} images.
    if board.is-board
      a.expand-link(href="#{@url}") Expand
  .replies
    for reply of @replies
      = post-template reply, \article \reply @@post-classes
